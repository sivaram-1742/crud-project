name: Deploy with Aramb

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  aramb-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set Github tag
        id: vars
        run: echo "sha_short=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT

      - name: Setup aramb
        uses: aramb-ai/aramb-setup@v0.0.2-beta1

      - name: Build and push
        timeout-minutes: 30
        run: aramb build --tag ${{ steps.vars.outputs.sha_short }}
        env:
          DOCKER_REGISTRY: ghcr.io
          DOCKER_REPOSITORY: ${{ github.repository }}
          DOCKER_USERNAME: sivaramclode
          DOCKER_PASSWORD: KingKong0400
          ARAMB_SERVICE_ID: f60258fb-5c2a-4c39-bc87-60f58291db82
          ARAMB_API_URL: https://nonscientifically-inapprehensive-barney.ngrok-free.dev
          ARAMB_API_TOKEN: ${{ secrets.ARAMB_API_TOKEN }}
